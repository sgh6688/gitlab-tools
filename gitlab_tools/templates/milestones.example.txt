# GitLab 根地址，不要带 /api/v4
gitlab_url=https://gitlab.example.com

# Token 可直接填写；更推荐保持为空并通过环境变量提供。
token=
token_env_var=GITLAB_TOKEN

# Milestone 和 Issue Markdown 导出目录
output_dir=D:\Downloads\ExportedByGitLabTools\Milestones

# 通用 API 请求配置
request_timeout_seconds=30
page_size=100
verify_ssl=true

# 逗号分隔，留空表示不启用该类目标。
groups=dept/platform-group
projects=dept/platform-group/project-a
